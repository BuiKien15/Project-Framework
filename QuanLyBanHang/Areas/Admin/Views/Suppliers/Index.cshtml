@model IEnumerable<QuanLyBanHang.Models.Supplier>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h2 class="mb-4">Suplliers table</h2>

            @if (ViewBag.SuccessMessage != null)
            {
                <div id="successMessage" class="alert alert-success">Source Action @ViewBag.SourceAction : @ViewBag.SuccessMessage</div>

                <script>
                    // Tự động ẩn thông báo thành công sau 3 giây
                    setTimeout(function () {
                        var successMessage = document.getElementById('successMessage');
                        if (successMessage) {
                            successMessage.style.display = 'none';
                        }
                    }, 3000);
                </script>
            }

            @if (ViewBag.ErrorMessage != null)
            {
                <div id="errorMessage" class="alert alert-danger">Source Action @ViewBag.SourceAction : @ViewBag.ErrorMessage</div>

                <script>
                    // Tự động ẩn thông báo lỗi sau 3 giây
                    setTimeout(function () {
                        var errorMessage = document.getElementById('errorMessage');
                        if (errorMessage) {
                            errorMessage.style.display = 'none';
                        }
                    }, 3000);
                </script>
            }

            <a asp-action="Create" type="button" class="btn mb-5 btn-gradient-primary btn-fw">Create New</a>

            <table id="table-products" class="table table-striped" style="width:100%">
                <thead>
                    <tr>
                        <th>
                            Supplier Name
                        </th>
                        <th>
                            Contact Name
                        </th>
                        <th>
                            Phone
                        </th>
                        <th>
                            Email
                        </th>
                        <th>
                            Number of Purchases
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.SupplierName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ContactName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Phone)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Email)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.NumberOfPurchases)
                            </td>
                            <td>
                                <div class="d-flex align-items-center justify-content-center">
                                    <a type="button" class="btn btn-sm btn-gradient-info" asp-action="Edit" asp-route-id="@item.SupplierId">Edit</a> |
                                    <a type="button" class="btn btn-sm btn-gradient-danger" asp-action="Delete" asp-route-id="@item.SupplierId">Delete</a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>
    </div>
</div>

